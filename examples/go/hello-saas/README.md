# hello-saas (Go)

Projects gauge through the framework API: create until the plan's cap trips (typed 402), then read the meters. See `../README.md` at the examples root for setup.

```bash
go run .   # SYNAPSE_TOKEN + SYNAPSE_ORG
```
