# hello-saas (Java)

Projects gauge through the framework API. SDK installed locally first (`cd sdk/java && mvn install`).

```bash
mvn -q compile exec:java -Dexec.mainClass=dev.synapse.example.HelloSaasSample
```
